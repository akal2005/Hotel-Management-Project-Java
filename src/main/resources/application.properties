# Server Port Configuration (matching REST API settings in frontend)
server.port=5000

# DataSource Config
spring.datasource.url=jdbc:mysql://localhost:3306/hotel_management_db?useSSL=false&serverTimezone=Asia/Kolkata&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Akalya
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Uploads max file limit sizes
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# JWT Properties (custom configs to be read in Java)
jwt.secret=dev_jwt_secret_hotelms_pro_2024_replace_in_prod
jwt.expires-in-ms=604800000
jwt.refresh-secret=dev_refresh_secret_hotelms_pro_2024_replace_in_prod
jwt.refresh-expires-in-ms=2592000000
