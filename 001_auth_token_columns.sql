-- ============================================================
-- MIGRATION 001 – Auth token columns (No-Op)
-- These columns are already defined in schema.sql, so this file is a no-op.
-- ============================================================

SELECT 'Migration 001 skipped (already in schema.sql)' AS status;

